# Salmon PMS — 操作手册（非技术人员版）

> **版本**: v1.0  
> **适用对象**: 完全不懂编程的用户，通过 OpenClaw Agent 完成所有技术操作  
> **核心原则**: 开发不影响线上，上线不影响使用，数据不丢失

---

## 🎯 一句话理解这套方案

**两个环境 + 功能分行开发 + 完成后合并上线**

- **生产环境（线上）**：员工每天使用的正式系统，必须稳定
- **开发环境（本地）**：用来开发新功能、做测试，坏了没关系
- **每个新功能**：单独开一条"功能分支"开发，做完再合到上线

---

## 📋 你需要知道的术语（极简版）

| 术语 | 人话解释 |
|------|---------|
| 分支 | 代码的"平行世界"，互相不干扰 |
| main 分支 | 最稳定的代码，对应线上系统 |
| dev 分支 | 开发中的代码，用来测试 |
| feature/XXX 分支 | 某个具体功能的代码，做完就删 |
| 合并 | 把一个分支的改动搬到另一个分支 |
| 部署 | 把代码放到服务器上，让大家能用 |
| 数据库备份 | 把数据复制一份，丢了能恢复 |

---

## 🔄 完整工作流程图

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  你想做新功能  │ ──→ │  Agent开发  │ ──→ │   测试验收   │
│ (说人话就行)  │     │ (feature分支)│     │  (dev环境)  │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                               │
                        ┌──────────────────────┘
                        ▼
               ┌─────────────┐     ┌─────────────┐
               │   合并上线    │ ──→ │  重新部署上线  │
               │  (main分支)  │     │  (生产环境)   │
               └─────────────┘     └─────────────┘
```

---

## 🛠️ 环境说明

### 生产环境（线上正式环境）

| 项目 | 说明 |
|------|------|
| 运行位置 | Docker Compose，在当前机器运行 |
| 数据库 | PostgreSQL（端口 5432） |
| 前端 | http://localhost:5173 |
| 后端 | http://localhost:8000 |
| 数据 | **真实业务数据，必须备份** |

### 开发环境（本地开发环境）

| 项目 | 说明 |
|------|------|
| 运行位置 | 本地直接运行 |
| 数据库 | SQLite（文件 `salmon_pms_dev.db`）或独立 PostgreSQL |
| 前端 | `pnpm dev`（端口 5173） |
| 后端 | `uvicorn`（端口 8000） |
| 数据 | 测试数据，可以随时重置 |

> **重要**：生产环境和开发环境不能同时运行，会端口冲突。Agent 会自动处理。

---

## 📱 你唯一需要做的：给 Agent 下指令

### 指令模板

```
"帮我开发一个 [功能名称] 功能"

例如：
- "帮我开发一个客户等级自动计算功能"
- "帮我做一个库存预警提醒功能"
- "帮我加一个按业务员统计销售业绩的功能"
```

### Agent 会自动帮你做的（你不用管）

1. ✅ 从 main 分支创建 `feature/功能名` 分支
2. ✅ 在开发环境启动后端和前端
3. ✅ 编写后端接口代码
4. ✅ 编写前端页面代码
5. ✅ 在开发环境测试功能是否正常
6. ✅ 告诉你测试完成了，让你验收
7. ✅（你确认后）合并到 dev 分支做更完整测试
8. ✅（你确认后）合并到 main 分支准备上线
9. ✅ 备份生产数据库
10. ✅ 重新部署生产环境
11. ✅ 验证线上功能正常

---

## ✅ 具体操作步骤（每次开发新功能）

### 第 1 步：提需求（你说，Agent 做）

**你说**："帮我开发一个 [具体功能描述]"

**Agent 会** ：
- 创建功能分支：`feature/xxx`
- 切换数据库到 SQLite（不碰线上数据）
- 启动开发环境
- 开始开发

**你等待**：复杂功能可能需要 5-30 分钟

---

### 第 2 步：开发中验收（Agent 给你看效果）

**Agent 完成代码后会**：
- 在开发环境测试 API 是否正常
- 给你看测试结果
- 如果有前端页面，会截图或描述效果

**你需要做的**：
- 看看功能是否符合你的预期
- **如果不满意**：告诉 Agent "这里要改..."，Agent 继续修改
- **如果满意**：告诉 Agent "可以了，进入下一步"

---

### 第 3 步：合并到 dev 测试（可选但推荐）

**你说**："合并到 dev 分支测试"

**Agent 会**：
- 把你的功能分支合并到 `dev` 分支
- 确保跟其他人的改动不冲突
- 运行基本检查

**你确认**："没问题"

---

### 第 4 步：准备上线（关键步骤）

**你说**："上线这个功能"

**Agent 会按顺序执行**：

#### 4.1 备份线上数据 ⚠️ 必须
```bash
# Agent 自动执行
./scripts/backup-db.sh
```
- 备份文件名：`backup_年月日_时分秒.sql`
- 保存在：`backups/` 文件夹

#### 4.2 合并代码到 main
```bash
git checkout main
git merge dev
```

#### 4.3 执行数据库迁移（如果有表结构变化）
```bash
cd backend
alembic upgrade head
```

#### 4.4 重新部署生产环境
```bash
cd docker
docker compose down  # 停掉旧的
docker compose up -d --build  # 启动新的
```

#### 4.5 验证上线成功
- Agent 会调用 API 检查功能是否正常
- Agent 会告诉你"上线成功"或"有问题需要回滚"

---

### 第 5 步：上线后验证（你做）

**打开浏览器**，访问 http://localhost:5173

**检查一下**：
- [ ] 原有功能是否正常（随便点几个页面）
- [ ] 新功能是否出现
- [ ] 新功能是否能正常使用
- [ ] 数据是否正确（看看关键数字对不对）

**如果发现问题**：
- 立刻告诉 Agent："上线有问题，回滚！"
- Agent 会自动用备份恢复数据

**如果一切正常**：
- 告诉 Agent："上线成功，清理功能分支"
- Agent 会删除已合并的 feature 分支，保持整洁

---

## ⚠️ 紧急情况：回滚操作

### 什么时候需要回滚？

- 上线后原有功能坏了
- 数据出现错误或不一致
- 新功能导致系统变慢或崩溃

### 怎么回滚？

**你说**："回滚到上线前"

**Agent 会自动**：

1. **代码回滚**：
   ```bash
   git checkout main
   git revert HEAD  # 撤销最后一次合并
   ```

2. **数据库回滚**：
   ```bash
   # 找到最近备份
   ls backups/backup_*.sql | tail -1
   # 恢复数据
   docker exec -i salmon-pms-db-1 psql -U salmon -d salmon_pms < backup_xxx.sql
   ```

3. **重新部署旧版本**：
   ```bash
   cd docker
   docker compose down
   docker compose up -d --build
   ```

4. **验证恢复**：检查原有功能是否正常

> **注意**：回滚会丢失上线后到回滚前这段时间录入的新数据。所以发现问题的**第一时间**就要回滚，不要等到第二天。

---

## 📅 日常维护（Agent 自动做）

### 每天自动备份

Agent 会在每天凌晨自动执行备份（需要配置定时任务）。

你也可以随时说："手动备份数据库"

### 每周检查

你可以每周问 Agent 一次：
- "检查一下系统状态"
- "看看有没有需要更新的"

Agent 会检查：
- 磁盘空间够不够
- 备份是否正常
- 有没有安全更新
- 系统运行日志有没有报错

---

## 📁 项目文件说明

| 文件/文件夹 | 用途 |
|------------|------|
| `backend/` | 后端代码（Python） |
| `frontend/` | 前端代码（React） |
| `docker/` | 部署配置 |
| `database/` | 数据库初始化脚本 |
| `scripts/` | 辅助脚本（备份、启动等） |
| `docs/` | 文档 |
| `memory/` | Agent 的工作记录 |
| `backups/` | 数据库备份存放处 |
| `OPERATIONS.md` | 本文件（操作手册） |
| `DEPLOYMENT.md` | 部署技术细节（Agent 看） |

---

## 🆘 常见问题

### Q: 我不在电脑旁边，能让 Agent 自己开发吗？

**A**: 不建议。开发过程中 Agent 可能会问你确认问题（比如"字段命名叫客户等级还是客户级别？"）。建议在旁边看着，或者让 Agent 先做完，你回来验收。

### Q: 开发到一半，我突然要关掉电脑怎么办？

**A**: 没问题。Agent 的改动都保存在代码里，关掉电脑只是暂停。下次开机后告诉 Agent "继续开发 [功能名]"，Agent 会从上次的地方继续。

### Q: 两个功能同时开发怎么办？

**A**: Agent 会创建两个功能分支（`feature/A` 和 `feature/B`），分别开发。但一次只能在一个分支上工作。先做完一个，合并上线，再做下一个。

### Q: 开发环境的数据怎么准备？

**A**: Agent 会自动从生产环境导出一部分脱敏数据，或者创建测试数据。你也可以说"复制生产数据到开发环境"（会去掉敏感信息）。

### Q: 上线后员工发现功能变了不习惯怎么办？

**A**: 
1. 提前跟员工说今天会更新
2. 选择员工不忙的时间上线（比如午休、下班后）
3. 新功能第一次出现时 Agent 可以加上"新功能"提示标签

### Q: 备份文件越来越多，磁盘满了怎么办？

**A**: 告诉 Agent "清理旧备份"，Agent 会自动保留最近 30 天的备份，删除更早的。

### Q: 我能同时在手机上用生产环境吗？

**A**: 目前项目运行在局域网内（localhost）。如果想手机访问，需要：
- 电脑和手机连同一个 WiFi
- 用电脑的局域网 IP 访问（如 `http://192.168.1.100:5173`）
- 或者后期部署到云服务器（可以找 Agent 帮忙）

---

## 📝 版本历史

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-26 | v1.0 | 初始版本，基于双环境 + 功能分支策略 |

---

## 💡 给你的建议

1. **不要慌**：整个过程 Agent 会引导你，你只需要说"是/否/改这个"
2. **勤备份**：每次上线前确认 Agent 已经备份了数据库
3. **先测试**：新功能一定要在开发环境试过再上线
4. **有问题立刻说**：越早发现问题，越容易解决
5. **保留这个手册**：有问题先翻手册，再 ask Agent

---

> **最后提醒**：你不是一个人在搞技术，Agent 是你的技术合伙人。不懂就问，说人话就行。
