[Unit]
Description=Salmon PMS Frontend (Nginx)
After=network.target salmon-backend.service

[Service]
Type=simple
ExecStart=/usr/sbin/nginx -c /mnt/e/salmon-pms-deploy/nginx.conf -g 'daemon off;'
ExecReload=/usr/sbin/nginx -s reload
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
