# 2026-05-26 — 三文鱼PMS系统开发记录

## 成品定义模块重构（23:52-00:27）

### 设计完成
- 四级结构：系列(ProductSeries) → SPU(ProductTemplate) → 规格(ProductSpec) → SKU(ProductVariant×Brand)
- 新增表：
  - `product_series`：5个系列（纯享装/拼盘装/即食装/团购装/副产品）
  - `product_specs`：规格从SPU分离，支持同一SPU多规格
  - `variant_price_tiers`：客户分级价/渠道价/阶梯价
- 改造表：
  - `product_templates`：加 `series_id`，名称改为产品概念
  - `product_variants`：加 `spec_id`，保留 `template_id` 兼容
  - `template_boms`：加 `material_type` + `is_main`，支持全品类关联
  - `variant_accessories`：加 `accessory_type`（accessory/gift/sample）
- 向后兼容：旧字段保留，新字段 nullable
- 编码体系：`{品牌}-{系列}-{规格}-{序号}`

### 数据库迁移
- Alembic migration: `20260525_2352_f1a2b3c4d5e6`
- 执行成功：创建3张新表，改造4张现有表
- 数据迁移：
  - 插入5个系列（纯享装CX/拼盘装PP/即食装JS/团购装TG/副产品FB）
  - TP-001 鱼腩200g+中段200g → 纯享装系列
  - TP-002 中段130g+去尾甜虾15 → 拼盘装系列
  - 创建2条规格记录，关联2个SKU
- 验证结果：纯享装→鱼腩200g+中段200g→海兴悦/中挪SKU ✓

### 后端模型更新
- 文件：`backend/app/models/finished_products.py`
- 新增类：ProductSeries, ProductSpec, VariantPriceTier
- 改造类：ProductTemplate（加series_id/specs）, ProductVariant（加spec_id/price_tiers）, TemplateBOM（加material_type/is_main）, VariantAccessory（加accessory_type）
- 待完成：__init__.py导出新模型

### 待完成工作
1. __init__.py 导出 ProductSeries/ProductSpec/VariantPriceTier
2. API改造：新增系列/规格/价格层级接口
3. 前端改造：系列→SPU→规格→SKU选择流程
4. 拼盘装配套商品支持非三文鱼产品（甜虾/希鲮鱼籽）
5. 副产品独立管理（鱼头鱼尾鱼骨/鱼头赠品）

## 关键设计决策
- 实际产品矩阵：纯享装8规格/拼盘装2规格/即食装3规格/团购装2规格/副产品
- 未来SKU>20款，必须方案B（系列+规格分离）
- 拼盘装BOM需要打通全品类（三文鱼/甜虾/希鲮鱼籽/配料/包装）

## 文件清单
- `backend/app/models/finished_products.py` — 重构后模型
- `backend/app/models/finished_products_refactor.py` — 设计草案（可删除）
- `backend/migrations/versions/20260525_2352_f1a2b3c4d5e6_add_product_series_specs_and_price_tiers.py`

### 模型导出与后端验证（00:27-00:50）
- `__init__.py` 导出 ProductSeries/ProductSpec/VariantPriceTier
- `finished_products.py` 编译通过，无错误
- 后端重启成功，API响应正常
- 待完成：API端点（series/specs/price-tiers CRUD）+ 前端改造

### 00:50 状态
- 后端运行正常（PID 20138），所有关键API返回200
- 成品定义重构：数据库+模型已完成，API和前端待续
- 建议：API和前端部分可明天继续，或今晚快速完成基础CRUD
